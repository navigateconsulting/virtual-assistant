version: '3.0'

## Docker compose test file for UI Appdcmer

services:


#  ui_trainer:
#    container_name: ui-trainer
##    build: './ui-trainer'
#    ports:
#      - "8080:80"

  mongodb:
    image: mongo:latest
    container_name: "mongodb"
    environment:
      - MONGO_DATA_DIR=/data/db
      - MONGO_LOG_DIR=/dev/null
      - MONGODB_USER="eva"
      - MONGODB_PASS="evapass"
    volumes:
      - ./vol_chatbot_data/database/db:/data/db
    ports:
      - 27017:27017
    command: mongod --smallfiles --logpath=/dev/null # --quiet


  backend-trainer:
    container_name: backend-trainer
    build: './backend-trainer'
    ports:
      - "8089:8089"


volumes:
  skype_keys_vol:
