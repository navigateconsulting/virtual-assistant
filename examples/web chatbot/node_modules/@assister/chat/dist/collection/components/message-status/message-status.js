import { h, Host } from "@stencil/core";
export class MessageStatus {
    constructor() {
        this.state = 'pending';
    }
    render() {
        const status = {
            'none': false,
            'pending': h("ion-icon", { name: 'time', class: "pending" }),
            'delivered': h("chat-check-mark", { ticks: "one" }),
            'read': h("chat-check-mark", { ticks: "two" })
        }[this.state];
        return (h(Host, { class: this.state }, status));
    }
    static get is() { return "chat-message-status"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["message-status.css"]
    }; }
    static get styleUrls() { return {
        "$": ["message-status.css"]
    }; }
    static get properties() { return {
        "state": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "MessageState",
                "resolved": "\"delivered\" | \"none\" | \"pending\" | \"read\"",
                "references": {
                    "MessageState": {
                        "location": "import",
                        "path": "../../interfaces"
                    }
                }
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": ""
            },
            "attribute": "state",
            "reflect": false,
            "defaultValue": "'pending'"
        }
    }; }
}
