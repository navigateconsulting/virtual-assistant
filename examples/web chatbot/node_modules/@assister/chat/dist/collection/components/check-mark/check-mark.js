import { h } from "@stencil/core";
export class CheckMark {
    constructor() {
        this.ticks = 'one';
    }
    render() {
        const secondTick = [
            h("div", { class: "check-mark-second-stem" }),
            h("div", { class: "check-mark-second-kick" }),
            h("div", { class: "check-mark-kick-half" })
        ];
        return (h("div", { class: "container" },
            h("div", { class: "check-mark-stem" }),
            this.ticks === 'one' && h("div", { class: "check-mark-kick" }),
            this.ticks === 'two' && secondTick));
    }
    static get is() { return "chat-check-mark"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["check-mark.css"]
    }; }
    static get styleUrls() { return {
        "$": ["check-mark.css"]
    }; }
    static get properties() { return {
        "ticks": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "'one' | 'two'",
                "resolved": "\"one\" | \"two\"",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": ""
            },
            "attribute": "ticks",
            "reflect": false,
            "defaultValue": "'one'"
        }
    }; }
}
