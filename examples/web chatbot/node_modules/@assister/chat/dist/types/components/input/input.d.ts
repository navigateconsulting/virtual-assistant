import { EventEmitter } from '../../stencil.core';
import { TextareaChangeEventDetail } from '@ionic/core';
import { AssisterInputChangeEventDetail } from '../../interfaces';
export declare class Input {
    textEmpty: boolean;
    private textarea?;
    send: EventEmitter<AssisterInputChangeEventDetail>;
    handleTextChange(event: CustomEvent<TextareaChangeEventDetail>): void;
    handleEnter(event: KeyboardEvent): void;
    handleSend(event?: MouseEvent): void;
    render(): any;
}
