import { EventEmitter } from '../../stencil.core';
import { MessageTriangle, MessageDirection, IncomingEventDetail } from '../../interfaces';
declare function createElementsFromText(text: string): HTMLElement[];
export declare class Pane {
    mapInputTextToHtmlElements: typeof createElementsFromText;
    triangle: MessageTriangle;
    incoming: EventEmitter<IncomingEventDetail>;
    pane?: HTMLChatPaneElement;
    private conversation?;
    addMessage(direction: MessageDirection, text: any): HTMLChatMessageElement;
    addOutgoingMessage(text: string): Promise<HTMLChatMessageElement>;
    addIncomingMessage(text: string): Promise<HTMLChatMessageElement>;
    addCard({ text, image }: {
        text?: string;
        image?: string;
    }): Promise<HTMLElement>;
    render(): any[];
}
export {};
