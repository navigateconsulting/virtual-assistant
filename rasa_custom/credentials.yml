# Example credentials.yml file. Update this file with any connector and its credentials. This file is refered by
# rasa container. Also , during testing ensure ngrok container is started ref docker-compose.ngrok_example.yml
# this file is only intended for reference. For production use do not use ngrok.

botframework:
  app_id: ""
  app_password: ""